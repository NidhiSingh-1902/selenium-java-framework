# Browser Configuration
# Options: chrome | firefox | edge
browser=chrome

# Run in headless mode (no browser window)
headless=false

# Application URL to open on test start
base.url=https://www.google.com

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=15
page.load.timeout=30
